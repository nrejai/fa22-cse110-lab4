## Solutions to Lab 4 - Part 1 Questions
#### Nikki Rejai

1. values added:  20
2. final result:  20
3. values added:  20
4. The code returns an error because the variable result is block scoped, since it was initialized with the "let" keyword. So by attempting to access it outside the block it causes an error.
5. The code returns an error because the variable result is modified after assigning it, which cannot be done to a const.
6. The code returns an error because the variable result has a block scope and is assigned in another if statement, so it can't be accessed outside the if statement.